import math


def binByWidth(data, k):
    length = len(data)
    w = (max(data)-min(data)/k)

    for i in range(0, k+1):

    return


def binByFrequency():
    return


data = [0, 4, 12, 161, 18, 24, 26, 28]
bin = 3

binByWidth(data, bin)
